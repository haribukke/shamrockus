# Shamrock assignment

To start the server run `node server_multi_worker.js` and before that install dependencies using `npm install`

there are multiple servers that are present. please check [thought_process.md](./thought_process.md) to understand how each thing works.

Test cases are written for each server.

